const model1 = {
  yAxis: {
    axisLabel: {
      color: '#999'
    },
    axisLine: {
      // y轴
      show: false
    },
    axisTick: {
      // y轴刻度线
      show: false
    },
    splitLine: {
      // 网格线
      show: false
    }
  },
  xAxis: {
    axisLine: {
      lineStyle: {
        type: 'dashed',
        color: '#999'
      }
    }
  }
};

export const modelMap = {
  model1
};
